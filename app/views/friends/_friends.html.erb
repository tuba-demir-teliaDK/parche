<% unless @user.friends.empty? %>
	<% @user.friends.each do |u| %>
	<%= u.firstName %> <%= u.lastName %>
		<%= link_to "sss", user_path(u.id) %>
		<%= u.firstName %> <%= u.lastName %>
		Since <%= u.created_at.to_s(:long) %>
	<% end %>
<% else %>
	None
<% end %>