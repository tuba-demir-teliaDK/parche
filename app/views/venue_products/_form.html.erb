<%= form_for(@venue_product) do |f| %>
  <% if @venue_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@venue_product.errors.count, "error") %> prohibited this venue_product from being saved:</h2>

      <ul>
      <% @venue_product.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :fs_venue_id %><br />
    <%= f.text_field :fs_venue_id %>
  </div>
  <div class="field">
    <%= f.label :product_id %><br />
    <%= f.number_field :product_id %>
  </div>
 <div class="field">
    <%= f.label :checkin_count %><br />
    <%= f.text_field :checkin_count %>
  </div>
 <div class="field">
    <%= f.label :verified_item_id %><br />
    <%= f.number_field :verified_item_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
