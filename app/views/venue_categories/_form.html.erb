<%= form_for(@venue_category) do |f| %>
  <% if @venue_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@venue_category.errors.count, "error") %> prohibited this venue_category from being saved:</h2>

      <ul>
      <% @venue_category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :fs_category_id %><br />
    <%= f.text_field :fs_category_id %>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :status %><br />
    <%= f.number_field :status %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
